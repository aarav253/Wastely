# Optional: leave unset and the app runs fully anonymous (no login, no points).
# Get these from Supabase Dashboard -> Project Settings -> API.
# VITE_SUPABASE_ANON_KEY is safe to expose in the client -- it's protected by
# the row-level-security policies in supabase/schema.sql.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
