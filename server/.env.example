ANTHROPIC_API_KEY=sk-ant-...
API_PORT=4001
CLAUDE_MODEL=claude-sonnet-5

# Optional: leave unset and the app runs fully anonymous (no accounts, no points).
# Get these from Supabase Dashboard -> Project Settings -> API. SERVICE_ROLE is
# secret -- server-only, never expose it to the client.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
